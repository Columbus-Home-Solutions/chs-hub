# Env + secrets
.env
.env.local
.env.*.local
.dev.vars
*.pem
*.key

# Node / build
node_modules/
dist/
build/
.next/
.turbo/
.wrangler/

# Preact app build output (generated by `npm --prefix frontend run build`).
# Emitted into the static-assets root at public/app and served at /app;
# rebuilt on deploy, so not committed.
/public/app/

# Python (in case scripts land here)
__pycache__/
*.pyc
.venv/
venv/

# D1 backups / rollback safety nets (kept locally, never committed)
backup_pre_sprint1*.sql

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
scripts/dev-seed-sprint2.local.sql
scripts/dev-seed-sprint3.local.sql
scripts/dev-seed-sprint4.local.sql
scripts/*.local.sql
scripts/.e2e-cookie.local
backup_pre_sprint*_remote_*.sql
scripts/dev-seed-sprint6.local.sql
scripts/dev-seed-sprint8.local.sql
scripts/dev-seed-sprint10.local.sql
scripts/dev-seed-sprint11.local.sql
scripts/dev-seed-sprint12.local.sql
documents/
