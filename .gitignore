# Env + secrets
.env
.env.local
.env.*.local
.dev.vars
*.pem
*.key

# Node / build
node_modules/
dist/
build/
.next/
.turbo/
.wrangler/

# Preact app build output (generated by `npm --prefix frontend run build`).
# Emitted into the static-assets root at public/app and served at /app;
# rebuilt on deploy, so not committed.
/public/app/

# Python (in case scripts land here)
__pycache__/
*.pyc
.venv/
venv/

# D1 backups / rollback safety nets (kept locally, never committed)
backup_pre_sprint1*.sql

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
scripts/dev-seed-sprint2.local.sql
backup_pre_sprint2_remote_*.sql
